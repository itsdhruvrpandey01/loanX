server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/itsdhruvrpandey01/loanX-config.git
          default-label: main